# pytanie
Quick and easy library for working with GraphQL queries.

Currently in a POC phase. More coming soon.

To try the example run

```sh
scala-cli playground.scala
```

you can import it into metals after running

```sh
scala-cli setup-ide playground.scala
```
